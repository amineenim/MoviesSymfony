{% extends 'base.html.twig' %}
{% block title %}Movie Detail{% endblock %}
{% block body %}
    <h3>Movie Detail</h3>
    <h4>{{ movie.title }}</h4>
    <p>{{ movie.description }} , It was relead {{ movie.releaseYear }}</p>
    <ul> The main actores of the Movie are :</ul>
        {% for actor in movie.actors %}
            <li> {{ actor.name }} </li>
        {% endfor %}
{% endblock %}
 